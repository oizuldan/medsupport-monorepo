stages:
  - lint-and-test

lint-and-test:
  image: node:12-alpine
  stage: lint-and-test
  tags:
    - docker
  script:
    - echo $MNG_USR
    - echo $MNG_PSWD
    - echo $MNG_DB
    - yarn
    - yarn start
    - yarn compile
    - yarn ci
