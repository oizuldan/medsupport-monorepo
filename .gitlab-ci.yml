stages:
  - lint-and-test

lint-and-test:
  image: node:12-alpine
  stage: lint-and-test
  tags:
    - docker
  script:
    - yarn
    - git log -1 --pretty=%B | npx commitlint
    - yarn compile
    - yarn ci
